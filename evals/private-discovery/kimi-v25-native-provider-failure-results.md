# Kimi v25 native suite: HTTP 408 on the first forced-tool request

**The first candidate request returned complete HTTP 408 and stopped the suite before any model output could be assessed.** The application settled `provider_unavailable` with the upstream outcome unknown. No retry, output repair, further candidate request, judge, confirmation or session completion followed. One of eight families started; none completed or earned acceptance. Session B and the other seven families remain unrun, and all human ratings remain unset.

The 22,817-byte request matched the actual native context and schema-derived forced `submitDiscoveryTurn` tool before dispatch. The complete response contained 221 bytes and arrived within **237.190 seconds** from dispatch to native completion, below the **900-second application deadline**. There were no available choices, tool arguments or usage. Timing includes native completion handling; it is not pure provider latency or whole-conversation duration.

The harness forwarded the original response bytes unchanged to production but retained only their hash and selected completion metadata, **not the raw error body**. The specific upstream error, origin and cause remain undetermined. This evidence establishes neither successful forced-tool compatibility nor an unsupported-tool diagnosis. Output-schema validation and semantic review were not reached. A future harness change should retain bounded sanitized provider error-code and origin metadata when available; this closeout makes no such change and starts no new model phase.

Independent diagnosis and atomic-preservation review agreed with the result. The substantive interview and profile state exactly matched the pre-dispatch snapshot: **one participant message, zero assistant messages, zero cards, no pending confirmation, null summary, InitialDiscovery scope, an open version-1 session and an empty version-0 canonical profile**. Authorization metadata became **invalidated**. The entire lifecycle metadata is therefore not claimed unchanged.

The tested source was `29870891f6cf92d4a30c4a33e79071eba7b25a18`, tree `b2e33b1e1710747a75b5f8c4d863927f31e82a7c`, using prompt v25, policy v6 and tool `submit-discovery-turn-v2`. Independent implementation review passed. The [exact-source CI run](https://github.com/cill-i-am/meal-planner/actions/runs/34759048566) passed **1,512 Quality tests and 2 Synthetic media container tests**. Real Chromium checks on the production panel/client/CSS passed both scope actions, exact reload/retry, legacy read-only history and explicit unreadable-request recovery, including confirmation reconciliation. All 31 browser-consumed source files matched this commit. That browser fixture used synthetic sockets and actual browser storage; it does not establish authenticated native routing or provider quality.

The new call retains its full **511,182-microUSD reservation**, with no usage credit or billing claim. Adopted post-anchor exposure is **4,252,207 + 511,182 = 4,763,389 microUSD** against the original **20,000,000-microUSD** limit. The separate gross historical ledger has **157 reservations / 149 higher-cap reservations / 2 historical judge reservations / 23,816,002 microUSD**. These accounting measures are distinct from billed spend, which remains unknown.

Shutdown was graceful at **2026-09-13T13:48:07.372Z**. Runtime and actor joined with exit 0; all recorded processes and the listener were absent. One boundary capture and one report were retained, with no blocked request or ordinary network forwarding. All **960 source files and 98 harness pins** were verified unchanged after closure, before these documentation edits. Provider-side revocation was not verified.

The full family suite, candidate comparison, live A-to-B review, suite-wide fixed judges and actual human ratings remain required. This failed attempt does not change earlier results or establish candidate acceptance. The [implementation contract](../../docs/delivery/stages/02-private-discovery/03-deterministic-discovery-contract.md) records the deterministic guarantees and their semantic limits.

The [companion JSON](kimi-v25-native-provider-failure-results.json) retains selected receipt hashes, accounting and verification provenance without dialogue or raw provider content. Its SHA-256 is `a17941da1f7a9acf351fe0618e1beb3923a96b83b8232228657d3942f7fff589`.

# Typed private fallback discovery

This is the implementation design for [Work Item 03](03-adaptive-discovery-and-evaluation.md). The product owner authorized deterministic follow-up after the prompt-only experiment again ended before discovering the reported fallback and preparation constraints. Model recognition and extraction remain semantic tasks; the application owns the unanswered fields of a declared need.

## Contract and ownership

`private-discovery-needs.ts` owns a private `MealFallbackNeed`, its bounded updates and question selection. Each need has a stable app-owned ID, an attributed subject, declaration and subject evidence, active/declined/withdrawn state and three required fields: reason, reported acceptable option and extra preparation. The application initializes fields as unanswered. A field is one of:

```ts
type Field<T> =
  | { _tag: "Unanswered"; reopenedBy: Evidence | null }
  | { _tag: "Answered"; value: T; evidence: Evidence; reopenedBy: Evidence | null }
  | { _tag: "NoInformation"; evidence: Evidence; reopenedBy: Evidence | null }
  | { _tag: "Declined"; evidence: Evidence };
```

Reason and preparation retain the participant's description. An acceptable option explicitly distinguishes a generic option from an exact product and retains any stated quantity and substitution restriction. A generic option does not create a requirement for a brand or product. These values remain private context; they create no adult profile fact, dependant edit or approved fallback rule.

The model emits explicit declarations and field updates. The application derives each ID from the current participant message ID and declaration index. Updates refer to an existing ID or a declaration index from the same response; the model does not choose new IDs. Every new declaration, answer, correction, disposition, reopening or withdrawal cites the current participant message ID and an exact nonempty excerpt. The owning child verifies both against the actual current participant row before settlement. An old quote cannot replace a later correction. Retained previously validated state survives omission and the source message leaving the bounded model window. Excerpt matching establishes provenance, not semantic truth. The application rejects a literal duplicate declaration with the same subject, message and excerpt, while allowing two subjects supported by one statement. It cannot prove that a later declaration under different evidence describes a distinct real-world need; semantic identity remains a model responsibility.

Corrections retain the need key and carry new evidence. Answered and no-information fields may be reopened with new participant evidence. A declined field requires explicit participant choice to revisit it, represented separately from the answer or no-information evidence. A single `SetFieldDisposition(no_information)` can carry that fresh revisit evidence and settle the field atomically; the retained NoInformation state preserves it and does not cause another question. Missing or stale revisit evidence rejects the update. Refusal or no-information closes only the named field and does not turn it into a known answer. Whole-need refusal and withdrawal also require current participant evidence and suppress that need's follow-up without blocking other adult topics. Returning a declined or withdrawn need to active requires fresh evidence before field changes. There is no understood/irrelevant disposition. A declaration such as needing an alternative on a particular occasion does not itself answer why the alternative is needed. An exact-product correction preserves previously supported product identity, quantity and scope when the participant refers to the retained product without restating its name.

## Canonical question and message policy

Prompt v22/policy v5 make each unresolved generic note the sole owner of its required `question`. Circumstance, answered, no-information, declined and withdrawn notes forbid that field. Complete keyed updates replace a note in place, so closing a topic removes its question. Omitted notes retain both state and question across restart; new notes append in response order. The model still recognizes generic uncertainty and supplies the question's meaning.

Prompt v23 identifies the speaking adult's own hard food constraints as a necessary generic discovery topic when actual HardConstraint/NoKnownHardConstraints facts, own disclosures or a settled answered/no-information/declined note do not address it. It uses one concise unresolved question and the existing note states. Explicit absence, no information and refusal are distinct; the last two establish no safety clearance. Another person's answer cannot settle this adult's topic. Answered/no-information topics reopen only for new relevant information; refusal requires explicit choice. This adds no typed safety state or exhaustive category checklist.

Prompt v24 adds brief ordinary-meal understanding to open-ended discovery when practical meal context is missing; a liked food alone does not supply it. The model keeps one concise unresolved question, builds on disclosed circumstances and honors settled topics. Explicit limited profile edits do not receive this extra topic. The existing private-note states and question policy apply; no routine fact, new state or category checklist is introduced.

The model reply is `{ _tag: "Continue" }` or `{ _tag: "Stop", evidence }`. There is no model-authored text, generic question selector, Ask or Review. Removed fields reject strictly rather than being ignored or repaired.

After applying valid updates, the application selects the reply in this order:

1. A current-evidenced Stop allows no proposed change or continuity update, preserves the exact snapshot and renders fixed stop wording.
2. For the first active need with unanswered fields, ask its reason first, then acceptable option and preparation together when both remain unanswered, or only the missing field. Scoped no-information/refusal and whole-need refusal/withdrawal remain settled.
3. Once typed fields are addressed, ask the first retained unresolved note's question.
4. With neither unanswered active typed fields nor unresolved notes, permit review readiness. This does not complete the session.

`reviewPrivateDiscoveryProposals` validates the model's closed card operations against the actual profile and stored cards. Its reviewed records feed `applyPrivateDiscoveryContinuation` as a required argument, alongside current continuity, updates, reply and current participant message. That one pure flow returns the next snapshot, decision and fully rendered message. Native settlement and inspection use the same exported functions.

Application wording distinguishes an actual new draft from revision of an existing draft and describes its reviewed encoded effect as an unconfirmed profile proposal. Only those profile proposals receive an interface review invitation. A changed typed need receives a short, explicitly private-context summary preserving attribution, generic/exact option, quantity, substitution scope and manageable preparation when present; unanswered or unspecified values are not invented. Refusal, no-information and withdrawal retain their separate meanings. A current explicit correction or reaffirmation updates the affected field's evidence even when its interpreted value is unchanged, so it receives an acknowledgement without replaying unrelated needs. Omitted unchanged needs are not summarized again.

Typed extraction and generic question meaning still require semantic evaluation. Evidence establishes provenance and templating establishes the source of status wording; neither proves every model-authored value or question true.

## Persistence, bounds and failures

The existing continuity snapshot becomes one strict object, `{ notes, mealFallbackNeeds }`, stored in the same successful-turn `summary` TEXT column. It retains at most twelve notes and three needs within 8,192 UTF-8 bytes. Note updates retain their six-update bound; typed updates have a separate six-operation bound. The existing 24,576-byte context and 32,768-byte serialized request limits still apply. Oversize input fails; there is no eviction, fallback decoder, database migration or backfill. New evaluations use fresh sessions.

`private-assistant-turns.ts` validates new evidence, applies notes and need updates, derives the reply, then commits the assistant message, proposed cards and full snapshot in its existing transaction. Duplicate operations, unknown need keys, incorrect message references/excerpts, invalid note/reply shapes and state overflow reject the entire generated turn before any of those writes. The fully rendered message, including actual draft effects, private typed summaries, separators and the selected question, must fit 2,000 characters. It is validated before any assistant message, card, session-version or snapshot write; there is no clipping. Omitted values and restart retain the last valid snapshot.

The current prompt is v24; the model/state contract remains policy v5. Shared evidence, need references, profile-card changes and the canonical food-preference/profile-fact schemas use standard JSON Schema definitions without changing accepted values or validation to keep all three provider request shapes within their existing input bounds. The prompt assigns typed reason/option/preparation questions only to the typed need, avoiding duplicate generic unresolved notes for the same fields. Kimi thinking, sampling, completion allowance, deadline and whole-response limit remain fixed. The strict provider envelope, profile proposal review, participant authority and canonical confirmation path are unchanged.

## Verification

Pure cases cover declaration with no, partial or complete answers; unrelated safety updates; combined and single-field questions; omission; generic/exact option scope; correction; field-scoped refusal/no-information/revisit; Stop; unknown keys, stale evidence and limits. Native tests reuse the real child and existing atomic-settlement/restart fixtures, including a valid card combined with an invalid need update. The household-boundary proof exercises the authenticated turn endpoint and verifies that typed private context alone creates no canonical facts. The initial local native runs were setup-blocked by sandbox loopback denial and missing pinned dependency artifacts; they establish no acceptance. After exact dependency restoration, two native restart cases exposed request-size rejection when a proposed card was retained. Shared canonical fact-schema definitions removed duplication without widening input limits or changing accepted values. Both exact failures then passed.

Initial local verification on `92c3f53` passed 109 adapter/continuity/typed-policy tests, all 169 native private-output and household-boundary cases, 24 household API tests and 10 private-interview API tests. API and household API type checks, scoped lint/formatting and evaluation-asset validation passed. The native tests use local synthetic provider responses. Those tests did not establish live model-quality acceptance.

Independent review found that a declined field could not atomically settle an explicit revisit with no information. The scoped correction adds current revisit evidence to `SetFieldDisposition(no_information)` and retains it in NoInformation; it preserves the need ID and other fields. Positive and absent/stale/mismatched-evidence tests pass, including a native restart and omitted-update check plus atomic rejection with a valid proposed card. The corrected scope passed 113 focused tests, all 87 private-output native tests, API types and scoped lint/formatting. That correction retained prompt/policy v21/v4 and existing input/provider limits.

The v21 live attempts did not establish quality: the first ended with a provider failure and unknown upstream outcome; the second returned valid structured output but failed the separate generic-follow-up reference guard. Its misleading invitation to review private context was rejected before publication. The v22 ownership change removes both duplicated question routing and model-authored persistence/interface prose. Later verification and live results belong to their own source heads; historical failures remain unchanged.

V22 local checks pass 132 focused schema/policy/rendering tests and all 88 private-output native cases. All 85 household-boundary cases passed the v22 wire/renderer conversion, including the authenticated turn route with unchanged canonical profile. A proposed-card restart initially exceeded the native request-body limit; combining identical reason/preparation schema branches and removing redundant prompt sentences restored headroom without changing accepted operations, limits or rendered acknowledgement. API types, scoped lint/formatting and asset validation are checked on the final source. Live quality and immutable-head acceptance remain separate gates.

The v22 live run established four native successes, exact fallback/preparation discovery and the fixed correction challenge, but omitted the adult's safety question. Coverage was three of four, with the session open and no judge or family acceptance. V23 changes only the coverage prompt and prompt-version provenance, preserving the complete wire, renderer and policy. Local contract/size checks and native semantic results remain separate evidence.

V23 local validation passes the existing 132 focused tests, three native revisit/restart/request-bound cases, API types, scoped lint/formatting and asset validation. The prompt-only serialized increase is 472 bytes: the unchanged previously measured restart context calculates to a 32,497-byte body, leaving 271 bytes under the existing bound. The native case passes; this is no universal request-fit or semantic-completion claim.

The [v23 focused native test](../../../../evals/private-discovery/kimi-v23-safety-coverage-focused-results.md) on `52d1b2305a0bf31196266e78b382bfe46d4863e8` passed all four required discoveries and the fixed challenge across four successful candidate calls. Root and independent review passed all eleven hard assertions. Two explicitly confirmed own-adult facts reached profile version 2, and the native session completed at version 13 with eight retained messages. The fixed GPT-OSS judge scored household specificity and profile synthesis 5/5, with no possible hard failure flagged. The runtime closed and final source, harness and receipt verification passed. Both hosted CI checks on that source passed, including 1,491 tests across 114 files.

This is focused evidence for the declared-need policy and own-adult safety coverage in one synthetic scenario. It does not establish general extraction or agenda reliability, the complete eight-family suite, completed candidate comparison, live A-to-B review, suite-wide judge evidence or the sixteen actual human ratings. No candidate or human baseline is accepted; Work Item 03 and draft PR #218 remain incomplete and not ready to merge.

The [subsequent v23 baseline](../../../../evals/private-discovery/kimi-v23-native-baseline-coverage-results.md) passed two native turns and same-card correction but omitted ordinary-meal discovery, leaving coverage at two of three. V24 changes only the opening guidance and prompt-version provenance. Its 132 existing focused tests, four native revisit/restart/fresh-session cases, API types and scoped lint/format checks pass. A measured local synthetic Qwen request with a retained card after restart used 32,574 body bytes, leaving 194 bytes under the existing limit; this is one actual request, not a universal fit guarantee.

The [v24 live retest](../../../../evals/private-discovery/kimi-v24-native-output-failure-results.md) passed its opening and retained a usual-meals question, but never asked it. The complete second response omitted a required continuity-note `subject` and failed production output-schema validation. The entire generated turn was rejected: the original proposed card, successful continuity and empty profile remained unchanged, with three messages and a null failed-turn summary. The participant's safety answer and correction persist in the third message. No confirmation, completion, B, judge or other family ran. This failed result establishes neither full coverage nor prompt effectiveness; passing source checks and CI do not establish live acceptance. The runtime is closed and the remaining release gates are unchanged.

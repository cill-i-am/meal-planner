diff --git a/lib/Apply.js b/lib/Apply.js
index a01c0faaf46fac0063f04303744900c94f52f637..4fc8d75e2a7a02aa9b04efef2060028878ed4f77 100644
--- a/lib/Apply.js
+++ b/lib/Apply.js
@@ -354,7 +354,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                 resourceType: node.resource.Type,
                 props: news,
                 attr,
-                bindings: excludeDeletedBindings(node.bindings),
+                bindings: bindingOutputs,
                 providerVersion: node.provider.version ?? 0,
                 downstream: node.downstream,
                 removalPolicy: node.resource.RemovalPolicy,
@@ -450,6 +450,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     ...node.state,
                     attr,
                     props: news,
+                    bindings: bindingOutputs,
                 });
             }
             else {
@@ -461,7 +462,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     resourceType: node.resource.Type,
                     props: news,
                     attr,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     providerVersion: node.provider.version ?? 0,
                     downstream: node.downstream,
                     removalPolicy: node.resource.RemovalPolicy,
@@ -639,7 +640,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     props: news,
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     downstream: node.downstream,
                     removalPolicy: node.resource.RemovalPolicy,
                 });
@@ -656,7 +657,7 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
                     props: news,
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: bindingOutputs,
                     downstream: node.downstream,
                     // Preserve the remaining backlog exactly as-is. GC is responsible for
                     // popping one generation at a time until the chain is exhausted.
@@ -904,7 +905,7 @@ const converge = Effect.fn(function* (plan, tracker, terminalStatuses, session,
                     props: newProps,
                     attr,
                     providerVersion: node.provider.version ?? 0,
-                    bindings: excludeDeletedBindings(node.bindings),
+                    bindings: newBindings,
                     downstream: node.downstream,
                     namespace,
                     removalPolicy: node.resource.RemovalPolicy,
diff --git a/lib/Cloudflare/D1/ApplyMigrations.js b/lib/Cloudflare/D1/ApplyMigrations.js
index ba1c3be608856556930a5895497c1071cec015c1..f2ccfa888c1a6ca6d532fd3a042d355e1894ffdd 100644
--- a/lib/Cloudflare/D1/ApplyMigrations.js
+++ b/lib/Cloudflare/D1/ApplyMigrations.js
@@ -1,5 +1,6 @@
 import * as d1 from "@distilled.cloud/cloudflare/d1";
 import * as Effect from "effect/Effect";
+import { importD1Database } from "./ImportDatabase.js";
 /**
  * Apply pending D1 migrations in order. Uses the wrangler-compatible
  * 3-column schema `(id TEXT PK, name TEXT, applied_at TEXT)`.
@@ -14,18 +15,23 @@ export const applyMigrations = (options) => Effect.gen(function* () {
             continue;
         const migrationId = nextSeq.toString().padStart(5, "0");
         nextSeq += 1;
-        // D1 over HTTP doesn't support transactions; run migration + record
-        // in a single batched query.
-        yield* executeSQL(accountId, databaseId, [
-            migration.sql,
-            `INSERT INTO ${migrationsTable} (id, name, applied_at) VALUES ('${migrationId}', '${migration.id}', datetime('now'));`,
-        ].join("\n"));
+        // SQL-file imports preserve compound statements and roll back the
+        // migration record when any statement fails.
+        yield* importD1Database({
+            accountId,
+            databaseId,
+            sqlData: [
+                ...splitMigrationStatements(migration.sql),
+                `INSERT INTO ${migrationsTable} (id, name, applied_at) VALUES ('${migrationId}', '${migration.id}', datetime('now'));`,
+            ].join("\n"),
+        });
     }
 });
 const executeSQL = (accountId, databaseId, sql) => Effect.gen(function* () {
     const queryDb = yield* d1.queryDatabase;
     return yield* queryDb({ accountId, databaseId, sql });
 });
+const splitMigrationStatements = (sql) => sql.split(/--> statement-breakpoint(?:\r?\n)?/u).map((statement) => statement.trim()).filter((statement) => statement.length > 0);
 const detectSchema = (accountId, databaseId, migrationsTable) => Effect.gen(function* () {
     const result = yield* executeSQL(accountId, databaseId, `PRAGMA table_info(${migrationsTable});`).pipe(Effect.option);
     const columns = [];
diff --git a/lib/Cloudflare/Queues/Consumer.d.ts b/lib/Cloudflare/Queues/Consumer.d.ts
index bec1997c5fc2cfe7f7d6554848ca93118831f2d1..bd414ce0adf35e028aaef2ee660c4a32bef1e318 100644
--- a/lib/Cloudflare/Queues/Consumer.d.ts
+++ b/lib/Cloudflare/Queues/Consumer.d.ts
@@ -16,6 +16,14 @@ export type ConsumerProps = {
      * Optional dead letter queue name for failed messages.
      */
     deadLetterQueue?: string;
+    /**
+     * Physical ID of the dead letter queue.
+     *
+     * Required for the guarded delete-and-recreate compatibility fallback when
+     * Cloudflare accepts an update but omits the requested dead letter queue on
+     * readback.
+     */
+    deadLetterQueueId?: string;
     /**
      * Consumer settings.
      */
diff --git a/lib/Cloudflare/Queues/Consumer.js b/lib/Cloudflare/Queues/Consumer.js
index baf95aedd6b6fd2c4126f9ac4fe1b15c231a2bd4..c9e683c31c22a6dc303e3a97da589addc28cb7e5 100644
--- a/lib/Cloudflare/Queues/Consumer.js
+++ b/lib/Cloudflare/Queues/Consumer.js
@@ -63,6 +63,22 @@ const findWorkerConsumer = (acct, queueId) => queues.listConsumers.items({ accou
 // consumer yet" — the caller falls through to createConsumer, which
 // retries QueueNotFound until the queue propagates.
 Effect.catchTag("QueueNotFound", () => Effect.succeed(undefined)));
+const observeWorkerConsumer = Effect.fn(function* (options) {
+    if (isLiveId(options.consumerId)) {
+        const fetched = yield* queues
+            .getConsumer({
+            accountId: options.accountId,
+            queueId: options.queueId,
+            consumerId: options.consumerId,
+        })
+            .pipe(Effect.catchTag("ConsumerNotFound", () => Effect.succeed(undefined)));
+        const observed = fetched ? toObserved(fetched) : undefined;
+        if (observed) {
+            return observed;
+        }
+    }
+    return yield* findWorkerConsumer(options.accountId, options.queueId);
+});
 export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
     // The `consumerId` is not marked as stable because if you start in dev mode, the ID will change on first deploy.
     stables: ["accountId"],
@@ -104,13 +120,13 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
         Effect.catchTag(["QueueNotFound", "InvalidRoute"], () => Effect.succeed([]))), { concurrency: 10 });
         return rows.flat();
     }),
-    diff: Effect.fn(function* ({ olds, news, output }) {
+    diff: Effect.fn(function* ({ news, output }) {
         const { accountId } = yield* yield* CloudflareEnvironment;
         if (!isResolved(news))
             return undefined;
-        // If either contain `dev:` IDs, we need to update to live ones.
-        // The live resource doesn't exist yet, so there's no need to replace even when we otherwise would.
-        if (!isLiveId(output?.queueId) || !isLiveId(output?.consumerId)) {
+        // A dev queue does not exist in the live account, so there is
+        // nothing physical to observe yet.
+        if (!isLiveId(output?.queueId)) {
             return { action: "update" };
         }
         if ((output?.accountId ?? accountId) !== accountId) {
@@ -121,6 +137,21 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
         if (output?.queueId && news.queueId !== output.queueId) {
             return { action: "replace", deleteFirst: true };
         }
+        // Stable resources are diffed directly from persisted state by
+        // Plan.make; provider.read is not called on that path. Observe the
+        // physical consumer here so out-of-band DLQ/settings drift cannot be
+        // hidden by optimistic cached props. This is read-only and remains
+        // Consumer-scoped.
+        const observed = yield* observeWorkerConsumer({
+            accountId,
+            queueId: output.queueId,
+            consumerId: output.consumerId,
+        });
+        if (!observed ||
+            !isLiveId(output.consumerId) ||
+            observed.consumerId !== output.consumerId) {
+            return { action: "update" };
+        }
         // Settings / DLQ / script drift is an update. We DON'T
         // escalate scriptName changes to `replace` because the
         // engine resolves cross-resource Output<string> refs (a
@@ -132,16 +163,15 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
         // we detect script drift and rebuild the consumer in
         // place (Cloudflare's PUT silently ignores `script_name`
         // changes, so reconcile does delete-then-create).
-        if (JSON.stringify(olds.settings ?? {}) !==
-            JSON.stringify(news.settings ?? {}) ||
-            (olds.deadLetterQueue ?? undefined) !==
+        if (JSON.stringify(normalizeConsumerSettings(observed.settings)) !==
+            JSON.stringify(normalizeConsumerSettings(news.settings)) ||
+            observed.deadLetterQueue !==
                 (news.deadLetterQueue ?? undefined) ||
-            (output?.scriptName !== undefined &&
-                news.scriptName !== output.scriptName)) {
+            (observed.script !== undefined && news.scriptName !== observed.script)) {
             return { action: "update" };
         }
     }),
-    reconcile: Effect.fn(function* ({ news, output }) {
+    reconcile: Effect.fn(function* ({ news, olds, output }) {
         const { accountId } = yield* yield* CloudflareEnvironment;
         const acct = output?.accountId ?? accountId;
         // Only use `output.queueId` if it's a live ID (i.e. no `dev:` prefix).
@@ -302,19 +332,87 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
             while: (e) => e._tag === "QueueHandlerMissing",
             schedule: queueHandlerReadinessSchedule,
         }));
-        yield* queues.getConsumer({ accountId: acct, queueId, consumerId }).pipe(Effect.flatMap((fetched) => toObserved(fetched)?.script === news.scriptName
-            ? Effect.void
-            : Effect.fail("ScriptUnbound")), Effect.catchTag("ConsumerNotFound", () => Effect.fail("ScriptUnbound")), Effect.retry({
+        let converged = yield* queues
+            .getConsumer({ accountId: acct, queueId, consumerId })
+            .pipe(Effect.flatMap((fetched) => {
+            const observed = toObserved(fetched);
+            return observed?.script === news.scriptName
+                ? Effect.succeed(observed)
+                : Effect.fail("ScriptUnbound");
+        }), Effect.catchTag("ConsumerNotFound", () => Effect.fail("ScriptUnbound")), Effect.retry({
             while: (e) => e === "ScriptUnbound",
             schedule: queueHandlerReadinessSchedule,
         }));
+        const desiredDeadLetterQueue = news.deadLetterQueue ?? undefined;
+        const deadLetterQueueIdentityUnchanged = isLiveId(olds.deadLetterQueueId) &&
+            isLiveId(news.deadLetterQueueId) &&
+            olds.deadLetterQueueId === news.deadLetterQueueId;
+        const settingsConverged = JSON.stringify(normalizeConsumerSettings(converged.settings)) ===
+            JSON.stringify(normalizeConsumerSettings(news.settings));
+        const canReplaceForMissingDeadLetterQueue = deadLetterQueueIdentityUnchanged &&
+            owned &&
+            observed?.consumerId === consumerId &&
+            output?.consumerId === consumerId &&
+            observed.script === news.scriptName &&
+            observed.deadLetterQueue === undefined &&
+            JSON.stringify(normalizeConsumerSettings(observed.settings)) ===
+                JSON.stringify(normalizeConsumerSettings(news.settings)) &&
+            desiredDeadLetterQueue !== undefined &&
+            converged.consumerId === consumerId &&
+            converged.script === news.scriptName &&
+            converged.deadLetterQueue === undefined &&
+            settingsConverged;
+        if (canReplaceForMissingDeadLetterQueue) {
+            yield* verifyConsumerReplacementSafety({
+                accountId: acct,
+                consumerId,
+                deadLetterQueueId: news.deadLetterQueueId,
+                deadLetterQueueName: desiredDeadLetterQueue,
+                queueId,
+                scriptName: news.scriptName,
+                settings: news.settings,
+            });
+            yield* queues.deleteConsumer({
+                accountId: acct,
+                queueId,
+                consumerId,
+            });
+            yield* waitForConsumerDeletion(acct, queueId, consumerId);
+            const replacement = yield* queues.createConsumer({
+                accountId: acct,
+                queueId,
+                scriptName: news.scriptName,
+                type: "worker",
+                deadLetterQueue: desiredDeadLetterQueue,
+                settings: news.settings,
+            });
+            const replacementConsumerId = replacement.consumerId;
+            if (!isLiveId(replacementConsumerId) ||
+                replacementConsumerId === consumerId) {
+                return yield* Effect.fail("ConsumerReplacementUnconverged");
+            }
+            converged = yield* readConvergedConsumer({
+                accountId: acct,
+                consumerId: replacementConsumerId,
+                queueId,
+                scriptName: news.scriptName,
+            });
+            consumerId = replacementConsumerId;
+        }
+        if (converged.consumerId !== consumerId ||
+            converged.script !== news.scriptName ||
+            converged.deadLetterQueue !== desiredDeadLetterQueue ||
+            JSON.stringify(normalizeConsumerSettings(converged.settings)) !==
+                JSON.stringify(normalizeConsumerSettings(news.settings))) {
+            return yield* Effect.fail("ConsumerUnconverged");
+        }
         return {
-            consumerId,
+            consumerId: converged.consumerId,
             queueId,
-            scriptName: news.scriptName,
+            scriptName: converged.script,
             accountId: acct,
-            deadLetterQueue: news.deadLetterQueue,
-            settings: news.settings,
+            deadLetterQueue: converged.deadLetterQueue,
+            settings: converged.settings,
         };
     }),
     delete: Effect.fn(function* ({ output }) {
@@ -356,16 +454,15 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
                 consumerId: output.consumerId,
             })
                 .pipe(Effect.catchTag("ConsumerNotFound", () => Effect.succeed(undefined)));
-            if (fetched) {
+            const observed = fetched ? toObserved(fetched) : undefined;
+            if (observed) {
                 return {
-                    consumerId: fetched.consumerId,
+                    consumerId: observed.consumerId,
                     queueId: output.queueId,
-                    scriptName: ("scriptName" in fetched && typeof fetched.scriptName === "string"
-                        ? fetched.scriptName
-                        : output.scriptName) ?? output.scriptName,
+                    scriptName: observed.script ?? output.scriptName,
                     accountId: output.accountId,
-                    deadLetterQueue: output.deadLetterQueue,
-                    settings: output.settings,
+                    deadLetterQueue: observed.deadLetterQueue,
+                    settings: observed.settings,
                 };
             }
         }
@@ -381,8 +478,8 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
                     queueId: output.queueId,
                     scriptName: match.script ?? output.scriptName,
                     accountId: output.accountId,
-                    deadLetterQueue: output.deadLetterQueue,
-                    settings: output.settings,
+                    deadLetterQueue: match.deadLetterQueue,
+                    settings: match.settings,
                 };
             }
         }
@@ -390,8 +487,101 @@ export const ConsumerProviderLive = () => Provider.succeed(Consumer, {
     }),
 });
 const toObserved = (c) => c.consumerId && c.type === "worker"
-    ? { consumerId: c.consumerId, script: c.scriptName ?? undefined }
+    ? {
+        consumerId: c.consumerId,
+        deadLetterQueue: c.deadLetterQueue ?? undefined,
+        script: c.scriptName ?? undefined,
+        settings: c.settings
+            ? {
+                batchSize: c.settings.batchSize ?? undefined,
+                maxConcurrency: c.settings.maxConcurrency ?? undefined,
+                maxRetries: c.settings.maxRetries ?? undefined,
+                maxWaitTimeMs: c.settings.maxWaitTimeMs ?? undefined,
+                retryDelay: c.settings.retryDelay ?? undefined,
+            }
+            : undefined,
+    }
     : undefined;
+const normalizeConsumerSettings = (settings) => ({
+    batchSize: settings?.batchSize ?? 10,
+    maxConcurrency: settings?.maxConcurrency,
+    maxRetries: settings?.maxRetries ?? 3,
+    maxWaitTimeMs: settings?.maxWaitTimeMs ?? 5000,
+    retryDelay: settings?.retryDelay ?? 0,
+});
+const replacementUnsafe = (reason) => Effect.fail({
+    _tag: "ConsumerReplacementUnsafe",
+    reason,
+});
+const verifyConsumerReplacementSafety = Effect.fn(function* (options) {
+    if (!isLiveId(options.deadLetterQueueId)) {
+        return yield* replacementUnsafe("dead-letter-queue-id-missing");
+    }
+    const sourceQueue = yield* queues.getQueue({
+        accountId: options.accountId,
+        queueId: options.queueId,
+    });
+    const workerConsumers = (sourceQueue.consumers ?? []).flatMap((consumer) => {
+        const observed = toObserved(consumer);
+        return observed ? [observed] : [];
+    });
+    if (sourceQueue.queueId !== options.queueId ||
+        sourceQueue.producersTotalCount !== 0 ||
+        (sourceQueue.producers?.length ?? 0) !== 0 ||
+        sourceQueue.consumersTotalCount !== 1 ||
+        workerConsumers.length !== 1 ||
+        workerConsumers[0]?.consumerId !== options.consumerId ||
+        workerConsumers[0]?.script !== options.scriptName ||
+        workerConsumers[0]?.deadLetterQueue !== undefined ||
+        JSON.stringify(normalizeConsumerSettings(workerConsumers[0]?.settings)) !==
+            JSON.stringify(normalizeConsumerSettings(options.settings))) {
+        return yield* replacementUnsafe("source-queue-ownership-or-activity");
+    }
+    const sourceMetrics = yield* queues.getMetricsQueue({
+        accountId: options.accountId,
+        queueId: options.queueId,
+    });
+    if (sourceMetrics.backlogCount !== 0 || sourceMetrics.backlogBytes !== 0) {
+        return yield* replacementUnsafe("source-queue-backlog");
+    }
+    const deadLetterQueue = yield* queues.getQueue({
+        accountId: options.accountId,
+        queueId: options.deadLetterQueueId,
+    });
+    if (deadLetterQueue.queueId !== options.deadLetterQueueId ||
+        deadLetterQueue.queueName !== options.deadLetterQueueName ||
+        deadLetterQueue.producersTotalCount !== 0 ||
+        (deadLetterQueue.producers?.length ?? 0) !== 0 ||
+        deadLetterQueue.consumersTotalCount !== 0 ||
+        (deadLetterQueue.consumers?.length ?? 0) !== 0) {
+        return yield* replacementUnsafe("dead-letter-queue-identity-or-activity");
+    }
+    const deadLetterMetrics = yield* queues.getMetricsQueue({
+        accountId: options.accountId,
+        queueId: options.deadLetterQueueId,
+    });
+    if (deadLetterMetrics.backlogCount !== 0 ||
+        deadLetterMetrics.backlogBytes !== 0) {
+        return yield* replacementUnsafe("dead-letter-queue-backlog");
+    }
+});
+const waitForConsumerDeletion = (accountId, queueId, consumerId) => queues
+    .getConsumer({ accountId, queueId, consumerId })
+    .pipe(Effect.flatMap(() => Effect.fail("ConsumerStillAttached")), Effect.catchTag("ConsumerNotFound", () => Effect.void), Effect.retry({
+    while: (error) => error === "ConsumerStillAttached",
+    schedule: queueHandlerReadinessSchedule,
+}));
+const readConvergedConsumer = (options) => queues
+    .getConsumer(options)
+    .pipe(Effect.flatMap((fetched) => {
+    const observed = toObserved(fetched);
+    return observed?.script === options.scriptName
+        ? Effect.succeed(observed)
+        : Effect.fail("ScriptUnbound");
+}), Effect.catchTag("ConsumerNotFound", () => Effect.fail("ScriptUnbound")), Effect.retry({
+    while: (error) => error === "ScriptUnbound",
+    schedule: queueHandlerReadinessSchedule,
+}));
 // ~60s budget — Worker reconcile uploads typically land in 2–10s,
 // but a fresh container/asset deploy can stretch that.
 const queueHandlerReadinessSchedule = Schedule.max([
diff --git a/lib/Cloudflare/Queues/EventSource.d.ts b/lib/Cloudflare/Queues/EventSource.d.ts
index 0b609cf9379d242927208a40d24f11d9938f0b38..50613559495aa7d032bf0237de749834d20b17e7 100644
--- a/lib/Cloudflare/Queues/EventSource.d.ts
+++ b/lib/Cloudflare/Queues/EventSource.d.ts
@@ -4,6 +4,7 @@ import * as Duration from "effect/Duration";
 import * as Effect from "effect/Effect";
 import * as Layer from "effect/Layer";
 import * as Stream from "effect/Stream";
+import type { Input } from "../../Input.ts";
 import { Worker } from "../Workers/Worker.ts";
 import type { Queue } from "./Queue.ts";
 /**
@@ -30,7 +31,9 @@ export interface MessagesProps {
      */
     retryDelay?: Duration.Input;
     /** Optional dead-letter queue name. */
-    deadLetterQueue?: string;
+    deadLetterQueue?: Input<string>;
+    /** Physical ID paired with `deadLetterQueue` for guarded reconciliation. */
+    deadLetterQueueId?: Input<string>;
 }
 /**
  * Convert a {@link MessagesProps} (with `Duration.Input` time fields)
diff --git a/lib/Cloudflare/Queues/EventSource.js b/lib/Cloudflare/Queues/EventSource.js
index 4f79fdff1eebd98f888781ee9f3655750f72879b..cfab533151025e4320c51c75eca91b1d78470d68 100644
--- a/lib/Cloudflare/Queues/EventSource.js
+++ b/lib/Cloudflare/Queues/EventSource.js
@@ -68,6 +68,7 @@ export const EventSourceLive = Layer.effect(EventSource, Effect.gen(function* ()
                     scriptName: host.workerName,
                     settings: toConsumerSettings(props),
                     deadLetterQueue: props.deadLetterQueue,
+                    deadLetterQueueId: props.deadLetterQueueId,
                 });
             }));
         }
diff --git a/lib/Cloudflare/Workers/WorkerProvider.js b/lib/Cloudflare/Workers/WorkerProvider.js
index 320992f8eec3bd3319666ef8f793cc1700ff6c6e..4826e7c20163f698e57d67ae7f4fbabc9462b412 100644
--- a/lib/Cloudflare/Workers/WorkerProvider.js
+++ b/lib/Cloudflare/Workers/WorkerProvider.js
@@ -288,17 +288,24 @@ const workerAssetConfigForHash = (assets) => {
  * the bundle/vite/asset-content hashes, so a change to e.g. a compatibility
  * flag or observability config planned as a noop and silently never deployed.
  */
-const resolveWorkerMetadataHash = ({ props, bindings, accountId, stack, }) => resolveMetadataHashValue({
+/**
+ * @internal exported for provider-path regression testing.
+ */
+export const resolveWorkerMetadataHash = ({ props, bindings, accountId, stack, }) => resolveMetadataHashValue({
     accountId,
     stack: { name: stack.name, stage: stack.stage },
     compatibility: getCompatibility(props),
     env: props.env,
-    bindings: bindings.map((binding) => ({
-        sid: binding.sid,
-        data: binding.data,
-    })),
+    // Hash the live Worker metadata contributed by bindings, not each
+    // resource binding's whole internal data object. Resource providers add
+    // plan/apply-only fields (for example databaseId, bucketName, or a
+    // Container's local `dev` image) as their Outputs resolve. Those fields
+    // are not themselves sent to the Worker API and can differ between plan
+    // phases, which made an unchanged Worker plan as an update forever.
+    bindings: bindings.flatMap((binding) => binding.data.bindings ?? []),
+    containers: Array.from(new Set(bindings.flatMap((binding) => (binding.data.containers ?? []).map((container) => container.className)))).sort(),
     assets: workerAssetConfigForHash(props.assets),
-    cache: props.cache,
+    cache: props.cache ?? getCacheBinding(bindings),
     limits: props.limits,
     logpush: props.logpush,
     observability: props.observability,
@@ -1566,7 +1573,10 @@ export const LiveWorkerProvider = () => Provider.effect(Worker, Effect.gen(funct
         }),
         diff: Effect.fn(function* ({ id, news, olds, output, newBindings }) {
             const { accountId } = yield* yield* CloudflareEnvironment;
-            if (!isResolved(news))
+            // Plan calls diff once while resolving resource expressions and
+            // again after all binding Outputs resolve. Never hash the
+            // provisional binding shape from the first pass.
+            if (!isResolved(news) || !isResolved(newBindings))
                 return undefined;
             if ((output?.accountId ?? accountId) !== accountId) {
                 return { action: "replace" };
diff --git a/src/Apply.ts b/src/Apply.ts
index a025189e33b794e4288ffa2780033f0f311fbf72..8976101a998b319766b7a58c0740c9cc4907c7a0 100644
--- a/src/Apply.ts
+++ b/src/Apply.ts
@@ -680,7 +680,7 @@ const executeNode = (
           resourceType: node.resource.Type,
           props: news,
           attr,
-          bindings: excludeDeletedBindings(node.bindings),
+          bindings: bindingOutputs,
           providerVersion: node.provider.version ?? 0,
           downstream: node.downstream,
           removalPolicy: node.resource.RemovalPolicy,
@@ -802,6 +802,7 @@ const executeNode = (
             ...node.state,
             attr,
             props: news,
+            bindings: bindingOutputs,
           });
         } else {
           yield* commit<UpdatedResourceState>({
@@ -812,7 +813,7 @@ const executeNode = (
             resourceType: node.resource.Type,
             props: news,
             attr,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             providerVersion: node.provider.version ?? 0,
             downstream: node.downstream,
             removalPolicy: node.resource.RemovalPolicy,
@@ -1039,7 +1040,7 @@ const executeNode = (
             props: news,
             attr,
             providerVersion: node.provider.version ?? 0,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             downstream: node.downstream,
             removalPolicy: node.resource.RemovalPolicy,
           });
@@ -1055,7 +1056,7 @@ const executeNode = (
             props: news,
             attr,
             providerVersion: node.provider.version ?? 0,
-            bindings: excludeDeletedBindings(node.bindings),
+            bindings: bindingOutputs,
             downstream: node.downstream,
             // Preserve the remaining backlog exactly as-is. GC is responsible for
             // popping one generation at a time until the chain is exhausted.
@@ -1422,7 +1423,7 @@ const converge = Effect.fn(function* (
           props: newProps,
           attr,
           providerVersion: node.provider.version ?? 0,
-          bindings: excludeDeletedBindings(node.bindings),
+          bindings: newBindings,
           downstream: node.downstream,
           namespace,
           removalPolicy: node.resource.RemovalPolicy,
diff --git a/src/Cloudflare/D1/ApplyMigrations.ts b/src/Cloudflare/D1/ApplyMigrations.ts
index abf4a2e53a8793ad521ed70719bde153f72511de..8b763d891f00fc02056249ba5c5b1698a69a28b8 100644
--- a/src/Cloudflare/D1/ApplyMigrations.ts
+++ b/src/Cloudflare/D1/ApplyMigrations.ts
@@ -3,6 +3,7 @@ import * as d1 from "@distilled.cloud/cloudflare/d1";
 import * as Effect from "effect/Effect";
 import type * as HttpClient from "effect/unstable/http/HttpClient";
 import type { SqlFile } from "../../Sql/SqlFile.ts";
+import { importD1Database } from "./ImportDatabase.ts";

 export interface ApplyMigrationsOptions {
   accountId: string;
@@ -30,16 +31,16 @@ export const applyMigrations = (options: ApplyMigrationsOptions) =>
       if (applied.has(migration.id)) continue;
       const migrationId = nextSeq.toString().padStart(5, "0");
       nextSeq += 1;
-      // D1 over HTTP doesn't support transactions; run migration + record
-      // in a single batched query.
-      yield* executeSQL(
+      // SQL-file imports preserve compound statements and roll back the
+      // migration record when any statement fails.
+      yield* importD1Database({
         accountId,
         databaseId,
-        [
-          migration.sql,
+        sqlData: [
+          ...splitMigrationStatements(migration.sql),
           `INSERT INTO ${migrationsTable} (id, name, applied_at) VALUES ('${migrationId}', '${migration.id}', datetime('now'));`,
         ].join("\n"),
-      );
+      });
     }
   });

@@ -71,6 +72,12 @@ const executeSQL = (
     return yield* queryDb({ accountId, databaseId, sql });
   });

+const splitMigrationStatements = (sql: string): ReadonlyArray<string> =>
+  sql
+    .split(/--> statement-breakpoint(?:\r?\n)?/u)
+    .map((statement) => statement.trim())
+    .filter((statement) => statement.length > 0);
+
 const detectSchema = (
   accountId: string,
   databaseId: string,
diff --git a/src/Cloudflare/Queues/Consumer.ts b/src/Cloudflare/Queues/Consumer.ts
index e157f1fc5c8388c10039cc6c038f080712087cfc..7c64d9cfe43482e2def448c4cef95fb6d47543fa 100644
--- a/src/Cloudflare/Queues/Consumer.ts
+++ b/src/Cloudflare/Queues/Consumer.ts
@@ -30,6 +30,14 @@ export type ConsumerProps = {
    * Optional dead letter queue name for failed messages.
    */
   deadLetterQueue?: string;
+  /**
+   * Physical ID of the dead letter queue.
+   *
+   * Required for the guarded delete-and-recreate compatibility fallback when
+   * Cloudflare accepts an update but omits the requested dead letter queue on
+   * readback.
+   */
+  deadLetterQueueId?: string;
   /**
    * Consumer settings.
    */
@@ -137,6 +145,29 @@ const findWorkerConsumer = (acct: string, queueId: string) =>
     Effect.catchTag("QueueNotFound", () => Effect.succeed(undefined)),
   );

+const observeWorkerConsumer = Effect.fn(function* (options: {
+  accountId: string;
+  queueId: string;
+  consumerId?: string;
+}) {
+  if (isLiveId(options.consumerId)) {
+    const fetched = yield* queues
+      .getConsumer({
+        accountId: options.accountId,
+        queueId: options.queueId,
+        consumerId: options.consumerId,
+      })
+      .pipe(
+        Effect.catchTag("ConsumerNotFound", () => Effect.succeed(undefined)),
+      );
+    const observed = fetched ? toObserved(fetched) : undefined;
+    if (observed) {
+      return observed;
+    }
+  }
+  return yield* findWorkerConsumer(options.accountId, options.queueId);
+});
+
 export const ConsumerProviderLive = () =>
   Provider.succeed(Consumer, {
     // The `consumerId` is not marked as stable because if you start in dev mode, the ID will change on first deploy.
@@ -199,12 +230,12 @@ export const ConsumerProviderLive = () =>
       );
       return rows.flat();
     }),
-    diff: Effect.fn(function* ({ olds, news, output }) {
+    diff: Effect.fn(function* ({ news, output }) {
       const { accountId } = yield* yield* CloudflareEnvironment;
       if (!isResolved(news)) return undefined;
-      // If either contain `dev:` IDs, we need to update to live ones.
-      // The live resource doesn't exist yet, so there's no need to replace even when we otherwise would.
-      if (!isLiveId(output?.queueId) || !isLiveId(output?.consumerId)) {
+      // A dev queue does not exist in the live account, so there is
+      // nothing physical to observe yet.
+      if (!isLiveId(output?.queueId)) {
         return { action: "update" };
       }
       if ((output?.accountId ?? accountId) !== accountId) {
@@ -215,6 +246,23 @@ export const ConsumerProviderLive = () =>
       if (output?.queueId && news.queueId !== output.queueId) {
         return { action: "replace", deleteFirst: true } as const;
       }
+      // Stable resources are diffed directly from persisted state by
+      // Plan.make; provider.read is not called on that path. Observe the
+      // physical consumer here so out-of-band DLQ/settings drift cannot be
+      // hidden by optimistic cached props. This is read-only and remains
+      // Consumer-scoped.
+      const observed = yield* observeWorkerConsumer({
+        accountId,
+        queueId: output.queueId,
+        consumerId: output.consumerId,
+      });
+      if (
+        !observed ||
+        !isLiveId(output.consumerId) ||
+        observed.consumerId !== output.consumerId
+      ) {
+        return { action: "update" };
+      }
       // Settings / DLQ / script drift is an update. We DON'T
       // escalate scriptName changes to `replace` because the
       // engine resolves cross-resource Output<string> refs (a
@@ -227,17 +275,16 @@ export const ConsumerProviderLive = () =>
       // place (Cloudflare's PUT silently ignores `script_name`
       // changes, so reconcile does delete-then-create).
       if (
-        JSON.stringify(olds.settings ?? {}) !==
-          JSON.stringify(news.settings ?? {}) ||
-        (olds.deadLetterQueue ?? undefined) !==
+        JSON.stringify(normalizeConsumerSettings(observed.settings)) !==
+          JSON.stringify(normalizeConsumerSettings(news.settings)) ||
+        observed.deadLetterQueue !==
           (news.deadLetterQueue ?? undefined) ||
-        (output?.scriptName !== undefined &&
-          news.scriptName !== output.scriptName)
+        (observed.script !== undefined && news.scriptName !== observed.script)
       ) {
         return { action: "update" } as const;
       }
     }),
-    reconcile: Effect.fn(function* ({ news, output }) {
+    reconcile: Effect.fn(function* ({ news, olds, output }) {
       const { accountId } = yield* yield* CloudflareEnvironment;
       const acct = output?.accountId ?? accountId;
       // Only use `output.queueId` if it's a live ID (i.e. no `dev:` prefix).
@@ -436,28 +483,107 @@ export const ConsumerProviderLive = () =>
           }),
         );

-      yield* queues.getConsumer({ accountId: acct, queueId, consumerId }).pipe(
-        Effect.flatMap((fetched) =>
-          toObserved(fetched)?.script === news.scriptName
-            ? Effect.void
-            : Effect.fail("ScriptUnbound" as const),
-        ),
-        Effect.catchTag("ConsumerNotFound", () =>
-          Effect.fail("ScriptUnbound" as const),
-        ),
-        Effect.retry({
-          while: (e) => e === "ScriptUnbound",
-          schedule: queueHandlerReadinessSchedule,
-        }),
-      );
+      let converged = yield* queues
+        .getConsumer({ accountId: acct, queueId, consumerId })
+        .pipe(
+          Effect.flatMap((fetched) => {
+            const observed = toObserved(fetched);
+            return observed?.script === news.scriptName
+              ? Effect.succeed(observed)
+              : Effect.fail("ScriptUnbound" as const);
+          }),
+          Effect.catchTag("ConsumerNotFound", () =>
+            Effect.fail("ScriptUnbound" as const),
+          ),
+          Effect.retry({
+            while: (e) => e === "ScriptUnbound",
+            schedule: queueHandlerReadinessSchedule,
+          }),
+        );
+
+      const desiredDeadLetterQueue = news.deadLetterQueue ?? undefined;
+      const deadLetterQueueIdentityUnchanged =
+        isLiveId(olds.deadLetterQueueId) &&
+        isLiveId(news.deadLetterQueueId) &&
+        olds.deadLetterQueueId === news.deadLetterQueueId;
+      const settingsConverged =
+        JSON.stringify(normalizeConsumerSettings(converged.settings)) ===
+        JSON.stringify(normalizeConsumerSettings(news.settings));
+      const canReplaceForMissingDeadLetterQueue =
+        deadLetterQueueIdentityUnchanged &&
+        owned &&
+        observed?.consumerId === consumerId &&
+        output?.consumerId === consumerId &&
+        observed.script === news.scriptName &&
+        observed.deadLetterQueue === undefined &&
+        JSON.stringify(normalizeConsumerSettings(observed.settings)) ===
+          JSON.stringify(normalizeConsumerSettings(news.settings)) &&
+        desiredDeadLetterQueue !== undefined &&
+        converged.consumerId === consumerId &&
+        converged.script === news.scriptName &&
+        converged.deadLetterQueue === undefined &&
+        settingsConverged;
+
+      if (canReplaceForMissingDeadLetterQueue) {
+        yield* verifyConsumerReplacementSafety({
+          accountId: acct,
+          consumerId,
+          deadLetterQueueId: news.deadLetterQueueId,
+          deadLetterQueueName: desiredDeadLetterQueue,
+          queueId,
+          scriptName: news.scriptName,
+          settings: news.settings,
+        });
+
+        yield* queues.deleteConsumer({
+          accountId: acct,
+          queueId,
+          consumerId,
+        });
+        yield* waitForConsumerDeletion(acct, queueId, consumerId);
+
+        const replacement = yield* queues.createConsumer({
+          accountId: acct,
+          queueId,
+          scriptName: news.scriptName,
+          type: "worker",
+          deadLetterQueue: desiredDeadLetterQueue,
+          settings: news.settings,
+        });
+        const replacementConsumerId = replacement.consumerId;
+        if (
+          !isLiveId(replacementConsumerId) ||
+          replacementConsumerId === consumerId
+        ) {
+          return yield* Effect.fail("ConsumerReplacementUnconverged" as const);
+        }
+
+        converged = yield* readConvergedConsumer({
+          accountId: acct,
+          consumerId: replacementConsumerId,
+          queueId,
+          scriptName: news.scriptName,
+        });
+        consumerId = replacementConsumerId;
+      }
+
+      if (
+        converged.consumerId !== consumerId ||
+        converged.script !== news.scriptName ||
+        converged.deadLetterQueue !== desiredDeadLetterQueue ||
+        JSON.stringify(normalizeConsumerSettings(converged.settings)) !==
+          JSON.stringify(normalizeConsumerSettings(news.settings))
+      ) {
+        return yield* Effect.fail("ConsumerUnconverged" as const);
+      }

       return {
-        consumerId,
+        consumerId: converged.consumerId,
         queueId,
-        scriptName: news.scriptName!,
+        scriptName: converged.script,
         accountId: acct,
-        deadLetterQueue: news.deadLetterQueue,
-        settings: news.settings,
+        deadLetterQueue: converged.deadLetterQueue,
+        settings: converged.settings,
       };
     }),
     delete: Effect.fn(function* ({ output }) {
@@ -509,17 +635,15 @@ export const ConsumerProviderLive = () =>
               Effect.succeed(undefined),
             ),
           );
-        if (fetched) {
+        const observed = fetched ? toObserved(fetched) : undefined;
+        if (observed) {
           return {
-            consumerId: fetched.consumerId!,
+            consumerId: observed.consumerId,
             queueId: output.queueId,
-            scriptName:
-              ("scriptName" in fetched && typeof fetched.scriptName === "string"
-                ? fetched.scriptName
-                : output.scriptName) ?? output.scriptName,
+            scriptName: observed.script ?? output.scriptName,
             accountId: output.accountId,
-            deadLetterQueue: output.deadLetterQueue,
-            settings: output.settings,
+            deadLetterQueue: observed.deadLetterQueue,
+            settings: observed.settings,
           };
         }
       }
@@ -538,8 +662,8 @@ export const ConsumerProviderLive = () =>
             queueId: output.queueId,
             scriptName: match.script ?? output.scriptName,
             accountId: output.accountId,
-            deadLetterQueue: output.deadLetterQueue,
-            settings: output.settings,
+            deadLetterQueue: match.deadLetterQueue,
+            settings: match.settings,
           };
         }
       }
@@ -548,19 +672,175 @@ export const ConsumerProviderLive = () =>
   });

 type ObservedConsumer = {
+  deadLetterQueue: string | undefined;
   consumerId: string;
+  settings: ConsumerSettings | undefined;
   script: string | undefined;
 };

 const toObserved = (c: {
+  deadLetterQueue?: string | null;
   consumerId?: string | null;
+  settings?: {
+    batchSize?: number | null;
+    maxConcurrency?: number | null;
+    maxRetries?: number | null;
+    maxWaitTimeMs?: number | null;
+    retryDelay?: number | null;
+  } | null;
   scriptName?: string | null;
   type?: "worker" | "http_pull" | null;
 }): ObservedConsumer | undefined =>
   c.consumerId && c.type === "worker"
-    ? { consumerId: c.consumerId, script: c.scriptName ?? undefined }
+    ? {
+        consumerId: c.consumerId,
+        deadLetterQueue: c.deadLetterQueue ?? undefined,
+        script: c.scriptName ?? undefined,
+        settings: c.settings
+          ? {
+              batchSize: c.settings.batchSize ?? undefined,
+              maxConcurrency: c.settings.maxConcurrency ?? undefined,
+              maxRetries: c.settings.maxRetries ?? undefined,
+              maxWaitTimeMs: c.settings.maxWaitTimeMs ?? undefined,
+              retryDelay: c.settings.retryDelay ?? undefined,
+            }
+          : undefined,
+      }
     : undefined;

+const normalizeConsumerSettings = (
+  settings: ConsumerSettings | undefined,
+): Required<Pick<ConsumerSettings, "batchSize" | "maxRetries" | "maxWaitTimeMs">> &
+  Pick<ConsumerSettings, "maxConcurrency" | "retryDelay"> => ({
+  batchSize: settings?.batchSize ?? 10,
+  maxConcurrency: settings?.maxConcurrency,
+  maxRetries: settings?.maxRetries ?? 3,
+  maxWaitTimeMs: settings?.maxWaitTimeMs ?? 5000,
+  retryDelay: settings?.retryDelay ?? 0,
+});
+
+const replacementUnsafe = (reason: string) =>
+  Effect.fail({
+    _tag: "ConsumerReplacementUnsafe",
+    reason,
+  } as const);
+
+const verifyConsumerReplacementSafety = Effect.fn(function* (options: {
+  accountId: string;
+  consumerId: string;
+  deadLetterQueueId?: string;
+  deadLetterQueueName: string;
+  queueId: string;
+  scriptName: string;
+  settings?: ConsumerSettings;
+}) {
+  if (!isLiveId(options.deadLetterQueueId)) {
+    return yield* replacementUnsafe("dead-letter-queue-id-missing");
+  }
+
+  const sourceQueue = yield* queues.getQueue({
+    accountId: options.accountId,
+    queueId: options.queueId,
+  });
+  const workerConsumers = (sourceQueue.consumers ?? []).flatMap((consumer) => {
+    const observed = toObserved(consumer);
+    return observed ? [observed] : [];
+  });
+  if (
+    sourceQueue.queueId !== options.queueId ||
+    sourceQueue.producersTotalCount !== 0 ||
+    (sourceQueue.producers?.length ?? 0) !== 0 ||
+    sourceQueue.consumersTotalCount !== 1 ||
+    workerConsumers.length !== 1 ||
+    workerConsumers[0]?.consumerId !== options.consumerId ||
+    workerConsumers[0]?.script !== options.scriptName ||
+    workerConsumers[0]?.deadLetterQueue !== undefined ||
+    JSON.stringify(
+      normalizeConsumerSettings(workerConsumers[0]?.settings),
+    ) !== JSON.stringify(normalizeConsumerSettings(options.settings))
+  ) {
+    return yield* replacementUnsafe("source-queue-ownership-or-activity");
+  }
+
+  const sourceMetrics = yield* queues.getMetricsQueue({
+    accountId: options.accountId,
+    queueId: options.queueId,
+  });
+  if (sourceMetrics.backlogCount !== 0 || sourceMetrics.backlogBytes !== 0) {
+    return yield* replacementUnsafe("source-queue-backlog");
+  }
+
+  const deadLetterQueue = yield* queues.getQueue({
+    accountId: options.accountId,
+    queueId: options.deadLetterQueueId,
+  });
+  if (
+    deadLetterQueue.queueId !== options.deadLetterQueueId ||
+    deadLetterQueue.queueName !== options.deadLetterQueueName ||
+    deadLetterQueue.producersTotalCount !== 0 ||
+    (deadLetterQueue.producers?.length ?? 0) !== 0 ||
+    deadLetterQueue.consumersTotalCount !== 0 ||
+    (deadLetterQueue.consumers?.length ?? 0) !== 0
+  ) {
+    return yield* replacementUnsafe(
+      "dead-letter-queue-identity-or-activity",
+    );
+  }
+
+  const deadLetterMetrics = yield* queues.getMetricsQueue({
+    accountId: options.accountId,
+    queueId: options.deadLetterQueueId,
+  });
+  if (
+    deadLetterMetrics.backlogCount !== 0 ||
+    deadLetterMetrics.backlogBytes !== 0
+  ) {
+    return yield* replacementUnsafe("dead-letter-queue-backlog");
+  }
+});
+
+const waitForConsumerDeletion = (
+  accountId: string,
+  queueId: string,
+  consumerId: string,
+) =>
+  queues
+    .getConsumer({ accountId, queueId, consumerId })
+    .pipe(
+      Effect.flatMap(() =>
+        Effect.fail("ConsumerStillAttached" as const),
+      ),
+      Effect.catchTag("ConsumerNotFound", () => Effect.void),
+      Effect.retry({
+        while: (error) => error === "ConsumerStillAttached",
+        schedule: queueHandlerReadinessSchedule,
+      }),
+    );
+
+const readConvergedConsumer = (options: {
+  accountId: string;
+  consumerId: string;
+  queueId: string;
+  scriptName: string;
+}) =>
+  queues
+    .getConsumer(options)
+    .pipe(
+      Effect.flatMap((fetched) => {
+        const observed = toObserved(fetched);
+        return observed?.script === options.scriptName
+          ? Effect.succeed(observed)
+          : Effect.fail("ScriptUnbound" as const);
+      }),
+      Effect.catchTag("ConsumerNotFound", () =>
+        Effect.fail("ScriptUnbound" as const),
+      ),
+      Effect.retry({
+        while: (error) => error === "ScriptUnbound",
+        schedule: queueHandlerReadinessSchedule,
+      }),
+    );
+
 // ~60s budget — Worker reconcile uploads typically land in 2–10s,
 // but a fresh container/asset deploy can stretch that.
 const queueHandlerReadinessSchedule = Schedule.max([
diff --git a/src/Cloudflare/Queues/EventSource.ts b/src/Cloudflare/Queues/EventSource.ts
index 94fe8e726cbbe5f9c7ea92cd69f49112dc137c48..720bddc00fe7eea6e8f6dae990c089ae3b34031a 100644
--- a/src/Cloudflare/Queues/EventSource.ts
+++ b/src/Cloudflare/Queues/EventSource.ts
@@ -5,6 +5,7 @@ import * as Duration from "effect/Duration";
 import * as Effect from "effect/Effect";
 import * as Layer from "effect/Layer";
 import * as Stream from "effect/Stream";
+import type { Input } from "../../Input.ts";
 import * as Namespace from "../../Namespace.ts";
 import { RuntimeContext } from "../../RuntimeContext.ts";
 import type { FunctionContext } from "../../Serverless/Function.ts";
@@ -37,7 +38,9 @@ export interface MessagesProps {
    */
   retryDelay?: Duration.Input;
   /** Optional dead-letter queue name. */
-  deadLetterQueue?: string;
+  deadLetterQueue?: Input<string>;
+  /** Physical ID paired with `deadLetterQueue` for guarded reconciliation. */
+  deadLetterQueueId?: Input<string>;
 }

 /**
@@ -211,6 +214,7 @@ export const EventSourceLive = Layer.effect(
               scriptName: host.workerName,
               settings: toConsumerSettings(props),
               deadLetterQueue: props.deadLetterQueue,
+              deadLetterQueueId: props.deadLetterQueueId,
             });
           }),
         );
diff --git a/src/Cloudflare/Workers/WorkerProvider.ts b/src/Cloudflare/Workers/WorkerProvider.ts
index 6ae1973c0463c1e103f2de857c1736b96ffe8624..179d5c9d8bf51daeec866081b527ed75a2c0c611 100644
--- a/src/Cloudflare/Workers/WorkerProvider.ts
+++ b/src/Cloudflare/Workers/WorkerProvider.ts
@@ -398,7 +398,10 @@ const workerAssetConfigForHash = (assets: WorkerProps["assets"]) => {
  * the bundle/vite/asset-content hashes, so a change to e.g. a compatibility
  * flag or observability config planned as a noop and silently never deployed.
  */
-const resolveWorkerMetadataHash = ({
+/**
+ * @internal exported for provider-path regression testing.
+ */
+export const resolveWorkerMetadataHash = ({
   props,
   bindings,
   accountId,
@@ -409,12 +412,24 @@ const resolveWorkerMetadataHash = ({
     stack: { name: stack.name, stage: stack.stage },
     compatibility: getCompatibility(props),
     env: props.env,
-    bindings: bindings.map((binding) => ({
-      sid: binding.sid,
-      data: binding.data,
-    })),
+    // Hash the live Worker metadata contributed by bindings, not each
+    // resource binding's whole internal data object. Resource providers add
+    // plan/apply-only fields (for example databaseId, bucketName, or a
+    // Container's local `dev` image) as their Outputs resolve. Those fields
+    // are not themselves sent to the Worker API and can differ between plan
+    // phases, which made an unchanged Worker plan as an update forever.
+    bindings: bindings.flatMap((binding) => binding.data.bindings ?? []),
+    containers: Array.from(
+      new Set(
+        bindings.flatMap((binding) =>
+          (binding.data.containers ?? []).map(
+            (container) => container.className,
+          ),
+        ),
+      ),
+    ).sort(),
     assets: workerAssetConfigForHash(props.assets),
-    cache: props.cache,
+    cache: props.cache ?? getCacheBinding(bindings),
     limits: props.limits,
     logpush: props.logpush,
     observability: props.observability,
@@ -2174,7 +2189,10 @@ export const LiveWorkerProvider = () =>
           }),
         diff: Effect.fn(function* ({ id, news, olds, output, newBindings }) {
           const { accountId } = yield* yield* CloudflareEnvironment;
-          if (!isResolved(news)) return undefined;
+          // Plan calls diff once while resolving resource expressions and
+          // again after all binding Outputs resolve. Never hash the
+          // provisional binding shape from the first pass.
+          if (!isResolved(news) || !isResolved(newBindings)) return undefined;
           if ((output?.accountId ?? accountId) !== accountId) {
             return { action: "replace" };
           }
